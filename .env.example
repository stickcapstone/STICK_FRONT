# 로컬 개발 프록시 대상 (vite.config.ts에서 사용)
# .env.local 파일에 실제 값을 작성하세요 (.env.local은 gitignore됨)
DEV_PROXY_TARGET=http://localhost:8081

# Vercel 배포: vercel.json rewrite가 /api/v1 → 백엔드로 자동 프록시
# 별도 서버에 배포할 경우에만 아래 주석 해제 후 값 설정
# VITE_API_BASE_URL=/api/v1/
